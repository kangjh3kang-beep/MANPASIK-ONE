apiVersion: apps/v1
kind: Deployment
metadata:
  name: cartridge-store-service
  labels:
    app: cartridge-store-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cartridge-store-service
  template:
    metadata:
      labels:
        app: cartridge-store-service
        tier: backend
    spec:
      containers:
        - name: cartridge-store-service
          image: manpasik/cartridge-store-service:latest
          ports:
            - containerPort: 50079
              name: grpc
            - containerPort: 9100
              name: metrics
          envFrom:
            - configMapRef:
                name: manpasik-config
            - secretRef:
                name: manpasik-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50079"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50079"]
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: cartridge-store-service
spec:
  selector:
    app: cartridge-store-service
  ports:
    - port: 50079
      targetPort: 50079
      name: grpc
    - port: 9100
      targetPort: 9100
      name: metrics
