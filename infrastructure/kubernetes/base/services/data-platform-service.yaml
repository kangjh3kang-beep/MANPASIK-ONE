apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-platform-service
  labels:
    app: data-platform-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: data-platform-service
  template:
    metadata:
      labels:
        app: data-platform-service
        tier: backend
    spec:
      containers:
        - name: data-platform-service
          image: manpasik/data-platform-service:latest
          ports:
            - containerPort: 50080
              name: grpc
            - containerPort: 9100
              name: metrics
          envFrom:
            - configMapRef:
                name: manpasik-config
            - secretRef:
                name: manpasik-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50080"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50080"]
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: data-platform-service
spec:
  selector:
    app: data-platform-service
  ports:
    - port: 50080
      targetPort: 50080
      name: grpc
    - port: 9100
      targetPort: 9100
      name: metrics
