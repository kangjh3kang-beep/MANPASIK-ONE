apiVersion: apps/v1
kind: Deployment
metadata:
  name: cartridge-service
  labels:
    app: cartridge-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cartridge-service
  template:
    metadata:
      labels:
        app: cartridge-service
        tier: backend
    spec:
      containers:
        - name: cartridge-service
          image: manpasik/cartridge-service:latest
          ports:
            - containerPort: 50059
              name: grpc
            - containerPort: 9100
              name: metrics
          envFrom:
            - configMapRef:
                name: manpasik-config
            - secretRef:
                name: manpasik-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50059"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50059"]
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: cartridge-service
spec:
  selector:
    app: cartridge-service
  ports:
    - port: 50059
      targetPort: 50059
      name: grpc
    - port: 9100
      targetPort: 9100
      name: metrics
