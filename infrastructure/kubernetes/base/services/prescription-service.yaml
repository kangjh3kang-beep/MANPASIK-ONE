apiVersion: apps/v1
kind: Deployment
metadata:
  name: prescription-service
  labels:
    app: prescription-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: prescription-service
  template:
    metadata:
      labels:
        app: prescription-service
        tier: backend
    spec:
      containers:
        - name: prescription-service
          image: manpasik/prescription-service:latest
          ports:
            - containerPort: 50069
              name: grpc
            - containerPort: 9100
              name: metrics
          envFrom:
            - configMapRef:
                name: manpasik-config
            - secretRef:
                name: manpasik-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50069"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50069"]
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: prescription-service
spec:
  selector:
    app: prescription-service
  ports:
    - port: 50069
      targetPort: 50069
      name: grpc
    - port: 9100
      targetPort: 9100
      name: metrics
