apiVersion: apps/v1
kind: Deployment
metadata:
  name: shop-service
  labels:
    app: shop-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shop-service
  template:
    metadata:
      labels:
        app: shop-service
        tier: backend
    spec:
      containers:
        - name: shop-service
          image: manpasik/shop-service:latest
          ports:
            - containerPort: 50056
              name: grpc
            - containerPort: 9100
              name: metrics
          envFrom:
            - configMapRef:
                name: manpasik-config
            - secretRef:
                name: manpasik-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50056"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50056"]
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: shop-service
spec:
  selector:
    app: shop-service
  ports:
    - port: 50056
      targetPort: 50056
      name: grpc
    - port: 9100
      targetPort: 9100
      name: metrics
