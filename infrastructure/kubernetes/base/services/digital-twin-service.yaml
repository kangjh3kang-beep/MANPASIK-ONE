apiVersion: apps/v1
kind: Deployment
metadata:
  name: digital-twin-service
  labels:
    app: digital-twin-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: digital-twin-service
  template:
    metadata:
      labels:
        app: digital-twin-service
        tier: backend
    spec:
      containers:
        - name: digital-twin-service
          image: manpasik/digital-twin-service:latest
          ports:
            - containerPort: 50073
              name: grpc
            - containerPort: 9100
              name: metrics
          envFrom:
            - configMapRef:
                name: manpasik-config
            - secretRef:
                name: manpasik-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50073"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50073"]
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: digital-twin-service
spec:
  selector:
    app: digital-twin-service
  ports:
    - port: 50073
      targetPort: 50073
      name: grpc
    - port: 9100
      targetPort: 9100
      name: metrics
