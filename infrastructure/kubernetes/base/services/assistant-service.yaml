apiVersion: apps/v1
kind: Deployment
metadata:
  name: assistant-service
  labels:
    app: assistant-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: assistant-service
  template:
    metadata:
      labels:
        app: assistant-service
        tier: backend
    spec:
      containers:
        - name: assistant-service
          image: manpasik/assistant-service:latest
          ports:
            - containerPort: 50077
              name: grpc
            - containerPort: 9100
              name: metrics
          envFrom:
            - configMapRef:
                name: manpasik-config
            - secretRef:
                name: manpasik-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50077"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50077"]
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: assistant-service
spec:
  selector:
    app: assistant-service
  ports:
    - port: 50077
      targetPort: 50077
      name: grpc
    - port: 9100
      targetPort: 9100
      name: metrics
