apiVersion: apps/v1
kind: Deployment
metadata:
  name: calibration-service
  labels:
    app: calibration-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: calibration-service
  template:
    metadata:
      labels:
        app: calibration-service
        tier: backend
    spec:
      containers:
        - name: calibration-service
          image: manpasik/calibration-service:latest
          ports:
            - containerPort: 50060
              name: grpc
            - containerPort: 9100
              name: metrics
          envFrom:
            - configMapRef:
                name: manpasik-config
            - secretRef:
                name: manpasik-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50060"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50060"]
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: calibration-service
spec:
  selector:
    app: calibration-service
  ports:
    - port: 50060
      targetPort: 50060
      name: grpc
    - port: 9100
      targetPort: 9100
      name: metrics
