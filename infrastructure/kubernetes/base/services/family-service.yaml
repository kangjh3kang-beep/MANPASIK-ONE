apiVersion: apps/v1
kind: Deployment
metadata:
  name: family-service
  labels:
    app: family-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: family-service
  template:
    metadata:
      labels:
        app: family-service
        tier: backend
    spec:
      containers:
        - name: family-service
          image: manpasik/family-service:latest
          ports:
            - containerPort: 50063
              name: grpc
            - containerPort: 9100
              name: metrics
          envFrom:
            - configMapRef:
                name: manpasik-config
            - secretRef:
                name: manpasik-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50063"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50063"]
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: family-service
spec:
  selector:
    app: family-service
  ports:
    - port: 50063
      targetPort: 50063
      name: grpc
    - port: 9100
      targetPort: 9100
      name: metrics
