apiVersion: apps/v1
kind: Deployment
metadata:
  name: voice-profile-service
  labels:
    app: voice-profile-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: voice-profile-service
  template:
    metadata:
      labels:
        app: voice-profile-service
        tier: backend
    spec:
      containers:
        - name: voice-profile-service
          image: manpasik/voice-profile-service:latest
          ports:
            - containerPort: 50081
              name: grpc
            - containerPort: 9100
              name: metrics
          envFrom:
            - configMapRef:
                name: manpasik-config
            - secretRef:
                name: manpasik-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50081"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50081"]
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: voice-profile-service
spec:
  selector:
    app: voice-profile-service
  ports:
    - port: 50081
      targetPort: 50081
      name: grpc
    - port: 9100
      targetPort: 9100
      name: metrics
