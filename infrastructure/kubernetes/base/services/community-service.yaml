apiVersion: apps/v1
kind: Deployment
metadata:
  name: community-service
  labels:
    app: community-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: community-service
  template:
    metadata:
      labels:
        app: community-service
        tier: backend
    spec:
      containers:
        - name: community-service
          image: manpasik/community-service:latest
          ports:
            - containerPort: 50065
              name: grpc
            - containerPort: 9100
              name: metrics
          envFrom:
            - configMapRef:
                name: manpasik-config
            - secretRef:
                name: manpasik-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50065"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:50065"]
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: community-service
spec:
  selector:
    app: community-service
  ports:
    - port: 50065
      targetPort: 50065
      name: grpc
    - port: 9100
      targetPort: 9100
      name: metrics
