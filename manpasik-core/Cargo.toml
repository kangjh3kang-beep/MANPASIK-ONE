[package]
name = "manpasik-core"
version = "0.1.0"
edition = "2021"
description = "ManPaSik (萬波息) Core Engine"

[dependencies]
# Dependencies for standard error handling, serialization, etc.
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# NOTE: In the future, no_std might be configured here.
