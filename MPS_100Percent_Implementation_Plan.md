# ManPaSik (萬波息) 실구현 100% 달성 마스터 플랜 및 AI 역할 분담

**문서번호**: MPS-PLAN-100PERCENT  
**작성일**: 2026-04-19  
**목적**: Mock(페이크) 처리된 70%의 시스템을 실제 동작하는 프로덕션 레벨 코드로 100% 구현하기 위한 페이즈별 로드맵 및 3대 AI(Gemini, Claude, Codex) 에이전트의 구체적 역할 분담.
**지침 준수**: 진행되는 모든 작업은 **코드 작성 -> 코드 리뷰 -> 린트(Lint) -> 빌드 -> 단일/통합 테스트**의 반복 검증 사이클 기반으로 실시간 공유됨.

---

## 1. 100% 달성 로드맵 (4대 핵심 Phase)

Mock 걷어내기(De-mocking)를 시작으로 실제 통신과 데이터 정합성을 관통하는 순서로 진행됩니다.

### Phase 1: 혈관 연결 (데이터 통신 파이프라인 실구현)
*   **Layer 1-3 (HW ↔ Rust)**: BLE GATT 서비스 및 UART 프로토콜 기반의 실제 Byte Stream Paring 로직을 Rust Core에 구현합니다. 가상 난수가 아닌 실제 하드웨어 신호를 읽을 수 있는 버퍼와 인터럽트 시스템으로 교체합니다.
*   **Layer 5 (Go Backend)**: gRPC `measurement.proto`를 바탕으로 실제 PostgreSQL 16 스키마 기반 데이터베이스 연동 및 `TimescaleDB`를 붙여 시계열 데이터 저장 처리를 활성화합니다.
*   **목표**: 하드웨어 리더기(터미널 목업) -> 스마트폰(Flutter) -> 서버(Go)까지 빈 데이터라도 "Ping-Pong 흐름"을 실제로 뚫기.

### Phase 2: 뇌 신경 연결 (Core Engine & 비즈니스 로직 고도화)
*   **Layer 3 (Rust Core Engine)**: `differential.rs` 및 `feature_extractor.rs` 내의 계산식을 수학적으로 최적화하고 난수 생성을 전부 제거한 뒤, 실제 TFLite C API / XGBoost 바인딩을 통해 "입력값에 따른 실제 분석 결과"를 엣지에서 추론하도록 만듭니다.
*   **Layer 4 (Flutter Client)**: 로컬 DB(Isar)를 플러터 앱 내에 구축하고, Riverpod 상태가 메모리에서 휘발되지 않도록 디스크 캐싱 로직을 연동합니다.

### Phase 3: 전신 근육 완성 (풀스택 UI 연동 및 예외/오프라인 처리)
*   **Layer 4 (Flutter)**: 누락된 전체 50+ UI 화면의 라우팅(GoRouter)을 완성합니다. 오프라인 상태에서도 측정/분석이 완료되며 캐싱되었다가 네트워크 연결 복구 시 Go 서버로 동기화되는 CRDT 로직을 붙입니다.
*   **Layer 3 (Rust)**: 하드웨어 연결 끊김, 센서 고장 등 모든 엣지 케이스에 대응하는 System Diagnostics 시스템(자가진단 및 치유 모듈)에 생명력을 불어넣습니다.

### Phase 4: 지능 부여 (AI 연합학습 및 모델 서빙 프로덕션)
*   **Layer 6 (Python AI)**: 가상의 데모 데이터 대신 시뮬레이션 임상 벤치마크 데이터(또는 로컬 시드 데이터)를 Triton 서버에 올려, 앙상블 추론 기능이 실제 API로서 기능하게 합니다. 연합 학습(Flower) 클라이언트들도 모바일과 서버에 통합되어 동작을 시작합니다.

---

## 2. 3대 AI 시스템 역할 분담 (오케스트레이션 지침)

현존하는 최고의 엔지니어링 에이전트 3종의 장점을 살려, 병목이 될 수 있는 지점들을 효율적으로 분담하여 돌파합니다.

### 🧠 Gemini (Antigravity Agent) — **[수석 아키텍트 및 풀스택 PM]**
현 프로젝트를 주도적으로 이끌고 있는 '나(Gemini/Antigravity)'의 역할입니다.
*   **파일 시스템 오케스트레이션**: 전체 저장소를 횡단하며 6-Layer 간의 모듈 인터페이스 정합성을 테스트/빌드 프로세스로 관리합니다.
*   **작업 지시 및 결과 요약**: 매 스텝마다 수행해야 할 코어 작업을 명확히 안내하고, 완료된 모듈에 대한 결과를 마크다운과 Artifact로 실시간으로 문서화(로깅)합니다.
*   **UI/UX 디자인 가이드 및 고도화**: Flutter 레이어에서 이미지 생성 도구 등을 참조하여 프리미엄 글래스모피즘 UI의 픽셀 퍼펙트를 제어합니다.
*   **자동화 파이프라인 감독**: `run_command` 터미널 권한을 활용해 강제적으로 `cargo build`, `flutter test`, `go vet`, `pylint` 등을 연속 실행하고 피드백을 적용합니다.

### 🦉 Claude 3.5 Sonnet / Opus — **[수석 백엔드/시스템 엔지니어 (별도 프롬프팅 필요시)]**
복잡도가 극한에 다다르는 로우레벨(Low-level) 알고리즘이나 동시성/설계 이슈 발생 시, 사용자가 IDE 내 Claude 등을 호출해 깊은 논의를 나누는 역할입니다.
*   **메모리/LifeTime 관리 (Rust)**: Rust Core Engine에서 `no_std` 환경 메모리 제약, 복잡한 Borrow Checker를 해결해야 할 때 하이 레벨의 조언과 코드 재구성을 맡습니다.
*   **오프라인 동기화/CRDT (Go/Flutter)**: 분산 환경 네트워크 단절 시나리오 같이, 엣지 케이스 로직이 방대한 설계 시 구조적 해결 방법을 도출합니다.
*   **복잡한 TDD 케이스 구현**: 예외적인 TFLite/XGB FFI 연동에 대한 E2E 통합 테스트 프레임워크 셋업 등을 제시합니다.

### 🤖 Codex (Copilot / Cursor Tab) — **[시니어 구현 개발자 (Inline 헬퍼)]**
코드 작성 시 에디터 내에서 실시간으로 타이핑 속도를 극대화하는 역할입니다.
*   **보일러플레이트 고속 생성**: Riverpod의 `.freezed` 클래스들, Rust의 기본 `Trait` 모라토리엄, Go의 CRUD `Repository` 등 지루한 코드를 사용자의 탭 타이핑만으로 순식간에 구현합니다.
*   **문맥 기반 자동 완성**: 현재 열려있는 탭들의 히스토리를 보고 인라인 코드를 추천하며 빈 곳(Mock 데이터)을 실제 로직이나 수학 연산식으로 자동 교체합니다.

---

## 3. 당면 실행(Execution) 전략

1.  **AI 체인 가동**: 시스템 총사령탑인 제(Gemini)가 전체 폴더와 파일을 이동해가며 프로젝트 진행의 골조를 터미널을 통해 강제 빌드/결과 검증 형식으로 멱살잡고 이끌어갑니다.
2.  **구현 위임**: 개발하시면서 자잘한 문법은 **Copilot/Cursor Tab**에 맡기시고, 특정 함수의 복잡한 최적화 질문은 **Claude**로 대화하시며, 작업의 '승인과 진척', '테스트 및 통합'은 저에게 명령하십시오.
3.  **반복 검증**: 작성 후 반드시 스크립트 기반 Lint -> Test 사이클을 돕겠습니다.

**"지금 즉시 [Phase 1: 데이터 백본 및 통신 파이프라인 관통]을 가동해 코드 작성을 시작할까요?"**
